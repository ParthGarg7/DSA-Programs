#include <stdio.h>
#include <stdlib.h>

struct node
{
    struct node *back;
    int data;
    struct node *next;
} *head, *temp, *new_node;

int main()
{
    head = NULL;
    temp = head;

    while (1)
    {

     printf("press 1 for insertion\n");

        switch ()
        {
        case /* constant-expression */:
            /* code */
            break;
        
        default:
            break;
        }
    }

    return 0;
}